# 前端迁移计划分析：clwork/privahub/web vs sfwork/secretpad/frontend-src

## 一、架构对比概览

| 维度 | sfwork/secretpad/frontend-src | clwork/privahub/web |
|------|-------------------------------|---------------------|
| 框架 | umi (React) | Vite + TanStack Router |
| UI库 | Ant Design 5 | 自研 design-system |
| DAG引擎 | AntV X6 | 自研 dag-next (SVG) |
| 状态管理 | umi model (dva) | Zustand + React Query |
| 包管理 | pnpm monorepo (nx) | pnpm monorepo |
| 模块数 | ~50 modules + 110 dag files | 25 pages + 6 dag-next files |
| 可视化组件 | 11+ vis components | 5 vis components (ResultVisualization等) |
| 组件配置 | 完整配置面板 (binning/SCQL等) | AttributeForm + 复合类型 + SQL编辑器 ✅ |
| DAG执行模式 | 5种 (All/Single/Down/Up/Stop) | 5种 (All/Single/Down/Up/Stop) ✅ |
| 画布交互 | 复制/粘贴/删除/框选/快捷键/小地图 | 复制/粘贴/删除/快捷键/右键菜单/框选/小地图/全屏/导出导入/平移缩放 ✅ |

## 二、功能差异详细分析

### 2.1 DAG 结果可视化（最大差距 ⚠️）

**原版 (packages/dag/src/vis/)**：
- `output-table-result`：表格结果展示（支持多 sheet、高亮、CSV 下载）
- `binning`：WOE 分箱结果可视化（分箱表格 + 图表）
- `corr-matrix`：相关性矩阵热力图
- `dataset-sample`：数据集采样预览
- `equal-cut`：等频/等距分箱结果
- `feature-import`：特征重要性柱状图
- `mpc-pva-chart`：MPC PVA 图表
- `mpc-stats-info`：MPC 统计信息（面积图 + 连续/离散数据表）
- `psi`：PSI 结果可视化
- `regression-evaluation`：回归评估（分布图 + 指标表）
- `model-auto-submit`：模型自动提交

**新版现状 (packages/dag-next/)**：
- `ResultVisualization`：根据 output.type 自动分发渲染（table/chart/heatmap/metrics/tabs）
- `OutputTable`：增强表格（分页、列排序、CSV 导出）
- `StatsChart`：SVG 柱状图/折线图
- `KeyValuePanel`：键值对面板
- `CorrelationHeatmap`：相关性矩阵热力图
- ✅ 已覆盖原版核心可视化能力

### 2.2 组件配置面板

**原版 (modules/component-config/)**：
- `config-form-view.tsx`：动态表单渲染（基于 ComponentDef attrs）
- `config-modal.tsx`：配置弹窗
- `config-item-render/`：
  - 默认列选择、表选择、模型选择、节点选择
  - SQL 编辑器
  - `custom-render/binning-modification/`：完整分箱修改器（表格编辑、undo/redo、导出、合并、上传）
  - `custom-render/linear-model-parameters-modification/`：线性模型参数修改器
  - `custom-render/scql-editor/`：SCQL 查询编辑器
  - `custom-render/case-when-render/`：Case-When 规则编辑
  - `custom-render/groupby-render/`：GroupBy 聚合编辑
- `template-quick-config/`：模板快速配置（PSI、隐私、风险、脱敏、K匿名）

**新版现状**：
- `attribute-form.tsx`：增强动态表单（支持 str/int/float/bool/enum/list + 复合类型）
- **复合类型支持**：AT_SF_TABLE_COL（列选择）、AT_SF_TABLE（表选择）、AT_MODEL（模型选择）、AT_PARTY（参与方选择） ✅
- **异步数据加载**：通过 dataProvider 回调获取候选数据 ✅
- **多选标签控件**：AsyncMultiSelect（搜索、多选、标签展示） ✅
- **SQL 编辑器**：SqlEditor（语法高亮/行号/格式化/全屏/Tab缩进） ✅
- `component-interpreter.tsx`：组件定义解释器（只读展示）
- 无 binning 修改器、无快速配置

### 2.3 DAG 编辑画布

**原版 (modules/main-dag/ + packages/dag/)**：
- X6 画布：自定义节点形状、连线动画、小地图、缩放
- `graph-service.ts`：图数据服务（CRUD + 状态轮询）
- `graph-hook-service.ts`：Hook 服务（节点右键菜单、快捷键）
- `toolbar.tsx`：工具栏（保存、运行、停止、缩放、布局）
- `record.tsx`：运行记录视图
- `model-submission-entry.tsx`：模型提交入口
- `periodic-task-entry.tsx`：定时任务入口
- `dag-guide-tour`：引导式教程

**新版现状 (packages/dag-next/)**：
- SVG 画布：拖拽节点、连线、选中高亮（1360+ 行）
- 工具栏：保存、运行、缩放、自动布局
- **执行模式：Run All / Run Single / Run Down / Run Up / Stop** ✅
- **自动布局：拓扑排序 + 分层布局** ✅
- **键盘快捷键：Delete/Cmd+C/Cmd+V/Cmd+D** ✅
- **节点右键菜单：执行/复制/粘贴/删除** ✅
- **节点状态动画：Running 脉冲动画 + 进度条** ✅
- **状态颜色：Success 绿色 / Failed 红色 / Running 青色** ✅
- **小地图导航：SVG 缩略图显示所有节点和连线** ✅
- **框选功能：拖拽框选多个节点 + 高亮显示** ✅
- **全屏模式：一键切换全屏编辑** ✅
- **导出/导入 JSON：画布状态序列化与反序列化** ✅
- **运行记录时间线：ExecutionTimeline 组件 + 抽屉面板** ✅
- **画布平移：中键/Alt+左键拖拽平移** ✅
- **滚轮缩放：Ctrl/Cmd+滚轮缩放** ✅
- **重置视图：一键恢复默认缩放和偏移** ✅
- 右侧面板：配置/日志/输出 三 Tab

### 2.4 页面级差异

| 页面/模块 | 原版 | 新版 | 差距 |
|-----------|------|------|------|
| DAG 编辑器 | X6 + 完整工具链 | SVG 基础画布 | 交互深度 |
| 组件配置 | 完整面板 + 自定义渲染 | 基础表单 | 缺失高级配置 |
| 结果详情 | 11+ 可视化组件 | ResultVisualization 集成 ✅ | 已覆盖 |
| 运行记录 | dag-record 模块 | ExecutionTimeline 抽屉 ✅ | 已覆盖 |
| 模型管理 | model-manager 模块 | models 页面 | 功能等价 |
| 数据管理 | data-manager 模块 | data-tables/sources 页面 | 功能等价 |
| 引导教程 | guide-tour + guide-pipeline | guide 页面 + templates | 功能等价 |
| P2P 项目 | p2p-project-list/detail | p2p 页面 | 功能等价 |
| 消息中心 | message-center 模块 | messages 页面 | 功能等价 |
| 定时任务 | periodic-task 模块 | periodic-tasks 页面 | 功能等价 |
| 隐私场景 | privacy-scenes 模块 | privacy-scenes 页面 | 功能等价 |

### 2.5 新版独有功能

| 功能 | 说明 |
|------|------|
| dag-templates (14个) | 引导式模板向导（PSI/TEE/差分隐私/K匿名/数据分类等） |
| lineage | 数据血缘树 |
| model-pack | 模型打包弹窗 |
| scheduled-task-from-dag | 从 DAG 创建定时任务 |
| approval | 审批状态轮询 + 创建审批 |
| observability | Sentry + Web Vitals 监控 |
| design-system | 自研组件库（Button/Card/Modal/Badge/Toast等） |

## 三、迁移优先级

### P0 - 结果可视化增强（✅ 已完成）
1. ✅ 新增 `ResultVisualization` 组件：根据输出类型自动选择渲染方式
2. ✅ 新增 `OutputTable` 组件：增强表格展示（分页、列排序、CSV 导出）
3. ✅ 新增 `StatsChart` 组件：SVG 柱状图/折线图
4. ✅ 新增 `KeyValuePanel` 组件：键值对面板（模型指标/评估结果）
5. ✅ 新增 `CorrelationHeatmap` 组件：相关性矩阵热力图
6. ✅ 增强 DAG 页面输出面板：集成新可视化组件

### P1 - DAG 执行模式增强（✅ 已完成）
5. ✅ 新增 Run Single：执行单节点
6. ✅ 新增 Run Down：执行选中节点及其下游
7. ✅ 新增 Run Up：执行选中节点及其上游
8. ✅ 新增 Stop：停止执行
9. ✅ 新增 Tidy Layout：自动布局（拓扑排序 + 分层）
10. ✅ 图遍历工具：getDownstreamNodes / getUpstreamNodes

### P2 - 组件配置增强（✅ 已完成）
11. ✅ 增强 AttributeForm：支持 AT_SF_TABLE_COL、AT_SF_TABLE、AT_MODEL、AT_PARTY 复合类型
12. ✅ 添加 AsyncMultiSelect 多选标签控件（搜索、多选、标签展示）
13. ✅ 添加 AsyncSingleSelect 单选下拉控件
14. ✅ 添加 AttrDataProvider 数据提供器接口
15. ✅ 添加 SQL 编辑器组件（轻量级 SqlEditor，零依赖替代 Monaco）
16. 添加模板快速配置面板

### P3 - DAG 画布增强（✅ 已完成）
14. ✅ 键盘快捷键：Delete/Cmd+C/Cmd+V/Cmd+D
15. ✅ 节点右键菜单：执行/复制/粘贴/删除
16. ✅ 复制/粘贴/复制节点
17. ✅ 节点状态动画：Running 脉冲 + 进度条
18. ✅ 状态颜色区分：Success/Failed/Running
19. ✅ 小地图导航：SVG 缩略图显示节点和连线
20. ✅ 框选功能：拖拽框选多个节点 + 高亮显示
21. ✅ 运行记录时间线视图：ExecutionTimeline 组件
22. ✅ 全屏模式：一键切换全屏编辑
23. ✅ 导出/导入 JSON：画布状态序列化
24. ✅ 画布平移：中键/Alt+左键拖拽 + 滚轮缩放 + 重置视图
25. 添加 binning 修改器

## 四、代码改进清单

### 第一批：P0 结果可视化（✅ 已实施）

1. **`packages/dag-next/src/result-visualization.tsx`**（新增 522 行）：
   - `ResultVisualization`：根据 output.type 自动分发渲染（table/chart/heatmap/metrics/tabs）
   - `OutputTable`：增强表格（分页、列排序、CSV 导出、sticky header）
   - `StatsChart`：SVG 柱状图/折线图（自动缩放、坐标轴、tooltip）
   - `KeyValuePanel`：键值对面板（模型指标展示）
   - `CorrelationHeatmap`：相关性矩阵热力图（SVG 色阶渲染）

2. **`packages/dag-next/src/index.tsx`**（修改）：
   - 导出新组件（ResultVisualization, OutputTable, StatsChart, KeyValuePanel, CorrelationHeatmap）
   - `renderOutput` 改为调用 `ResultVisualization`
   - 输出面板容器样式优化

### 第二批：P1 DAG 执行模式（✅ 已实施）

3. **`packages/dag-next/src/index.tsx`**（增强）：
   - 新增 Props：`onRunSingle`, `onRunDown`, `onRunUp`, `onStopGraph`
   - 新增 Labels：`runSingle`, `runDown`, `runUp`, `stop`, `tidyLayout`, `selectNodeFirst`
   - 新增图遍历工具：`getDownstreamNodes()`, `getUpstreamNodes()`
   - 新增自动布局算法：`tidyLayout()`（拓扑排序 + 分层布局）
   - 工具栏新增按钮：Run Single / Run Down / Run Up / Stop / Tidy Layout

4. **`apps/secretpad/src/pages/dag/index.tsx`**（增强）：
   - 新增 `handleRunSingle`：执行单节点
   - 新增 `handleRunDown`：执行下游节点
   - 新增 `handleRunUp`：执行上游节点
   - 新增 `handleStopGraph`：停止执行
   - 传递新 Props 到 DAGNextWorkspace

5. **`apps/secretpad/src/shared/lib/i18n/dictionaries.ts`**（增强）：
   - 新增中英文翻译：runSingle, runDown, runUp, tidyLayout, selectNodeFirst

### 第三批：P3 画布交互增强（✅ 已实施）

6. **`packages/dag-next/src/index.tsx`**（增强）：
   - 新增键盘快捷键：Delete/Backspace 删除、Cmd+C 复制、Cmd+V 粘贴、Cmd+D 复制
   - 新增节点右键菜单：Run Single/Run Down/Run Up/Copy/Duplicate/Paste/Delete
   - 新增剪贴板机制：clipboardRef 存储复制的节点
   - 新增 handlePasteNode / handleDuplicateNode / handleCopyNode 函数

### 第四批：P2 组件配置增强 + 状态动画（✅ 已实施）

7. **`packages/dag-next/src/attribute-form.tsx`**（增强 200+ 行）：
   - 新增属性类型：AT_SF_TABLE_COL、AT_SF_TABLE、AT_MODEL
   - 新增 AttrDataProvider 接口：fetchColumns/fetchTables/fetchModels/fetchParties
   - 新增 AsyncMultiSelect 组件：多选标签控件（搜索、多选、标签展示）
   - 新增 AsyncSingleSelect 组件：异步单选下拉控件
   - 增强 AT_PARTY 渲染：支持 dataProvider 异步加载参与方列表

8. **`packages/dag-next/src/index.tsx`**（增强）：
   - 新增 attrDataProvider prop：传递复合类型数据提供器
   - 节点状态动画：Running 脉冲动画 (animate-pulse) + 青色边框
   - 节点状态颜色：Success 绿色 / Failed 红色 / Running 青色
   - 运行中节点进度条：显示 progress 百分比

9. **`apps/secretpad/src/pages/dag/index.tsx`**（增强）：
   - 新增 attrDataProvider 实现：fetchModels 从 API 获取模型列表
   - fetchParties 返回默认参与方 (alice/bob)

### 第五批：P3 小地图 + 框选 + 结果页可视化（✅ 已实施）

10. **`packages/dag-next/src/index.tsx`**（增强 60+ 行）：
    - 新增小地图导航：SVG 缩略图显示所有节点和连线，状态颜色区分
    - 新增框选功能：拖拽框选多个节点，实时高亮显示
    - 新增 selectedNodeIds 状态：多选节点集合
    - 新增 selectionRect 状态：框选矩形坐标
    - 新增多选计数提示：显示已选中节点数量

11. **`apps/secretpad/src/pages/results/index.tsx`**（增强）：
    - 集成 ResultVisualization 组件替代原始 JSON 渲染
    - 结果详情 Modal 支持表格/图表/热力图/指标等多种可视化

### 第六批：P3 运行记录 + 工具栏增强（✅ 已实施）

12. **`packages/dag-next/src/execution-timeline.tsx`**（新增 368 行）：
    - `ExecutionTimeline`：垂直时间线布局，显示状态图标、时间、耗时、进度
    - 支持 Running/Succeed/Failed/Stopped 四种状态
    - 支持点击选中记录、停止运行中的作业
    - 支持分页加载
    - 文案通过 labels 注入，保持可复用性

13. **`packages/dag-next/src/index.tsx`**（增强 50+ 行）：
    - 新增全屏模式：`isFullscreen` 状态 + `fixed inset-0 z-50` 样式
    - 新增导出 JSON：将画布状态序列化为文件下载
    - 新增导入 JSON：从文件读取并更新画布
    - 新增 labels：fullscreen/exitFullscreen/exportJson/importJson/records
    - 导出 ExecutionTimeline 组件

14. **`apps/secretpad/src/pages/dag/index.tsx`**（增强）：
    - 新增运行记录按钮：打开 ExecutionTimeline 抽屉
    - 新增 `isRecordsDrawerOpen` 状态
    - 新增 `executionRecords` 状态：从 API 加载作业历史
    - 集成 ExecutionTimeline 组件到抽屉面板

### 第七批：P2 SQL编辑器 + P3 画布平移缩放（✅ 已实施）

15. **`packages/dag-next/src/sql-editor.tsx`**（新增 320 行）：
    - `SqlEditor`：轻量级 SQL 编辑器，零外部依赖替代 Monaco Editor
    - 基础 SQL 语法高亮：关键字/字符串/注释/数字/函数名
    - 行号显示、Tab 缩进支持
    - 全屏切换、复制到剪贴板
    - SQL 格式化：关键字大写 + 主要子句换行缩进
    - 状态栏：行数/字符数统计

16. **`packages/dag-next/src/attribute-form.tsx`**（增强）：
    - AT_CUSTOM_PROTOBUF 类型智能分发：属性名含 sql/query/scql 时使用 SqlEditor
    - 其他 protobuf 属性仍使用普通 textarea

17. **`packages/dag-next/src/index.tsx`**（增强 30+ 行）：
    - 新增画布平移：`panOffset` 状态 + 中键/Alt+左键拖拽
    - 新增滚轮缩放：Ctrl/Cmd+滚轮调整缩放比例
    - 新增重置视图按钮：🎯 一键恢复 zoom=100% + pan=(0,0)
    - 新增 transform 容器：`translate(panX, panY) scale(zoom/100)`
    - 导出 SqlEditor 组件

## 五、技术债务

- dag-next 的 SVG 画布在节点数 > 50 时性能下降，长期需考虑 Canvas/WebGL
- 原版 X6 的自定义形状（圆形端口、动画连线）在新版中简化为矩形节点
- 原版的 i18n 通过 umi locale 管理，新版通过自建 I18nContext（功能等价）
