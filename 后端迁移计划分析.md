# 后端迁移计划分析：clwork/privahub (Go) vs sfwork/secretpad (Java)

## 一、架构对比概览

| 维度 | sfwork/secretpad (Java) | clwork/privahub (Go) |
|------|------------------------|---------------------|
| 框架 | Spring Boot 3 + JPA | Gin + GORM |
| 调度 | Quartz Scheduler | robfig/cron |
| Kuscia通信 | gRPC (protobuf) | HTTP REST (JSON) |
| 组件定义 | Protobuf ComponentDef | 静态 JSON 文件 |
| 模块数 | 6 Maven modules | 单 Go module |
| 认证 | JWT + ApiResourceAuth + DataResourceAuth | JWT + RBAC middleware |
| 部署模式 | Center/P2P/Edge | Center (P2P stub) |

## 二、功能差异详细分析

### 2.1 DAG 执行引擎（关键差距 ⚠️）

**Java 版**：
- `GraphBuilder`：基于输入/输出关系构建 DAG 拓扑，解析节点依赖
- `JobChain`：责任链模式（JobRenderHandler → JobPersistentHandler → JobSubmittedHandler）
- `KusciaJobConverter`/`TaskConverter`：将 NodeDef 参数转换为 Kuscia Task 配置
- `NodeDefAdapter`：适配器模式处理特殊组件参数（BinningModifications、ModelParamModifications）
- 支持 MaxParallelism 并行调度

**Go 版现状**：
- `StartGraph` 平铺创建所有 Task，无拓扑排序
- 硬编码 parties（alice/bob），不解析 NodeDef
- 无依赖关系传递，无并行调度

**改进方案**：实现 `GraphBuilder` 拓扑排序 + 依赖感知的 Kuscia Task 构建

### 2.2 节点管理深度

**Java 版**：
- 创建节点时同步注册 Kuscia Domain（含证书）
- `refreshNode` 从 Kuscia 查询 Domain 实时状态
- `getNodeToken` 通过 Kuscia 生成/刷新部署令牌
- 支持 TEE 节点列表、协作节点列表

**Go 版现状**：
- 创建节点时注册 Kuscia Domain（best-effort）✓
- `RefreshNode` 返回硬编码 "Ready" 状态 ✗
- Token 管理为本地 UUID，未对接 Kuscia ✗

**改进方案**：RefreshNode 查询 Kuscia Domain 状态；Token 对接 Kuscia Certificate API

### 2.3 组件服务

**Java 版**：
- 从 SecretFlow 组件 spec（protobuf）加载完整 ComponentDef
- 包含参数定义（attrs）、输入输出 schema、版本管理
- i18n 从国际化文件加载
- 支持组件校验（isSecretpadComponent）

**Go 版现状**：
- 7 个硬编码组件，无参数 schema
- i18n 硬编码中文
- 无版本管理

**改进方案**：从 `config/components.json` 加载完整组件定义（含 attrs/io schema）

### 2.4 定时任务

**Java 版**：
- Quartz 调度，每次执行创建独立 ProjectScheduleJobDO 记录
- 支持查询某次调度执行的所有 Job（listProjectJob）
- 事件驱动（ScheduledJobStartEvent）

**Go 版现状**：
- cron 调度，执行时调用 GraphService.StartGraph ✓
- `GetScheduledJobs` 返回空数组 ✗（未关联实际 Job）
- 无独立执行记录表 ✗

**改进方案**：创建 `project_schedule_job` 表，每次触发时记录 Job 关联

### 2.5 模型管理

**Java 版**：
- ModelExportService：完整的模型打包/导出流程（Kuscia Job + 状态轮询）
- ModelManagementService：模型分页、详情、Serving 部署
- 模型报告生成（ModelReportID）

**Go 版现状**：
- 模型 CRUD + Serving 部署 ✓
- ExportModel 创建 Kuscia Job 但无实际打包逻辑 ✗
- 无模型报告 ✗

### 2.6 数据代理（DataProxy）

**Java 版**：
- DataProxyService：管理 Kuscia DomainDatasource 的 DataProxy 配置
- 支持 Master/P2P 模式下的数据源代理更新

**Go 版**：完全缺失

### 2.7 证书服务

**Java 版**：
- CertificateService：通过 Kuscia gRPC 生成节点证书
- 用于节点注册时的身份认证

**Go 版**：
- `pkg/kuscia/certificate.go` 有 HTTP API 封装 ✓
- 但未在节点注册流程中集成 ✗

### 2.8 P2P 模式

**Java 版**：
- 完整的 P2P 项目/节点管理
- 跨节点数据同步（DataSyncController）
- 自治模式下的投票/审批

**Go 版**：
- P2P Handler 存在但为基础 stub
- 无实际跨节点通信

### 2.9 权限体系

**Java 版**：
- ApiResourceAuth：API 级别资源鉴权
- DataResourceAuth：数据级别鉴权（Inst/Project 维度）
- IP 黑名单（IpBlockConfig）

**Go 版**：
- JWT + 角色 RBAC 中间件
- 无资源级别鉴权

### 2.10 其他差异

| 功能 | Java | Go | 差距 |
|------|------|-----|------|
| 系统资源监控 (SysResourcesBizService) | ✓ | ✗ | 缺失 |
| 分区规则分析 (ReadPartitionRuleAnalysis) | ✓ | ✓ | 已实现 |
| 边缘数据同步 (EdgeDataSync + SSE) | ✓ | ✓ | 已实现 |
| OSS 存储 | ✓ | ✓ | 已实现 |
| 云日志 (SLS) | ✓ | ✓ | 已实现 |
| 投票同步 (VoteSync) | ✓ | ✓ | 基础实现 |
| 审批 (Approval) | ✓ | ✓ | 基础实现 |

## 三、迁移优先级

### P0 - 运行时正确性（本次实施）
1. ✅ DAG 拓扑排序 + 依赖感知调度
2. ✅ 节点状态从 Kuscia 实时刷新
3. ✅ 定时任务关联实际 Job 执行记录

### P1 - 功能完善（下一阶段）
4. 组件服务增强（完整 ComponentDef + attrs schema）
5. 证书服务集成到节点注册流程
6. DataProxy 服务实现
7. 模型打包/导出完整流程

### P2 - 深度对齐（长期）
8. P2P 跨节点通信
9. 资源级别鉴权（DataResourceAuth）
10. 系统资源监控
11. TEE 可信计算链路

## 四、本次代码改进清单

1. **`internal/service/graph_service.go`**：
   - 新增 `buildDAG()` 拓扑排序函数
   - `StartGraph` 改为依赖感知调度（按拓扑层级设置 MaxParallelism）
   - 从项目节点动态获取 parties（不再硬编码）

2. **`internal/service/node_service.go`**：
   - `RefreshNode` 改为查询 Kuscia Domain 实时状态

3. **`internal/service/scheduled_service.go`**：
   - 新增 `ProjectScheduleJobDO` 记录每次执行
   - `GetScheduledJobs` 返回实际关联的 Job 列表

4. **`internal/dao/model/scheduled.go`**：
   - 新增 `ProjectScheduleJobDO` 模型

## 五、风险评估

- DAG 拓扑排序需确保环路检测，避免死锁
- Kuscia 查询为 best-effort，网络不可达时降级为本地状态
- 定时任务 Job 关联需保证幂等性
